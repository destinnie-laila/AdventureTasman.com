import React from "react";

// This code acts as a 404 error for router if a user does not enter a valid path
const NoMatch = () => (
  <div>
    <h2>No Match</h2>
  </div>
);

export default NoMatch;
